import { NgDirectivesDirective } from './ng-directives.directive';

describe('NgDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
