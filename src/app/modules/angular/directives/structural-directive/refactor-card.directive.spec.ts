import { RefactorCardDirective } from './refactor-card.directive';

describe('RefactorCardDirective', () => {
  it('should create an instance', () => {
    const directive = new RefactorCardDirective();
    expect(directive).toBeTruthy();
  });
});
